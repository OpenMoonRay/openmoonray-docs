The USD primvar reader maps are part of the [USD Preview Surface](https://openusd.org/release/spec_usdpreviewsurface.html) 
spec.  They output primitive attributes from the geometry they are bound to.  The maps are separated by the types of
data that they read (i.e. *float*, *float2*, *float3*, *normal*, *point*, and *vector*).

