**DebugMap** is a utility for rendering built in data or attributes for the purpose of debugging. It can overlay a grid atop the data to visualize distortion.

Generally, it is recommended to use [render outputs]({{ "/user-reference/how-to-guides/render-outputs" | absolute_url }}) to view the same data without changing any shaders.
