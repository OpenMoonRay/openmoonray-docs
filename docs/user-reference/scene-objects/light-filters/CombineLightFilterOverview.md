---
The Combine light filter combines the effect of multiple light filters, with one of several modes:

- Multiply
- Add
- Subtract
- min()
- max()

The light filters are specified as an ordered list with the "light_filters" attribute.
ALL referenced light filters must exist in the LightFilterSet, not just the CombineLightFilter.

