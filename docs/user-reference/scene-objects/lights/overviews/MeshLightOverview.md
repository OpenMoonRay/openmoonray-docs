MeshLight allows an abitrary mesh of triangles to be turned into a light source.
The geometry for the mesh is specified via the light's _geometry_ attribute.

If the _parts_list_ attribute is present, only the listed parts of the mesh geometry will participate.
