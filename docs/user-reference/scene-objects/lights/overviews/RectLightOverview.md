RectLight is a flat rectangular light source. The rectangle lies in the light's local (x,y)-plane
where it has dimensions given by the _width_ and _height_ attributes.

Note that by default the light will be emitted from a single surface of the rectangle - the one corresponding to the
positive direction of the light's local z-axis. This behavior can be changed via the _sidedness_ attribute.

